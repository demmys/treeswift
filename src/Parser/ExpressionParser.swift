class ExpressionParser {
    private let ts: TokenStream

    init(_ ts: TokenStream) {
        self.ts = ts
    }

    func expressionList() -> [Expression] {
    }

    func expression() -> Expression {
    }

    func primaryExpression() -> Expression {
    }
}
